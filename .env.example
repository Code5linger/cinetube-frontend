# Public URL of this Next.js app (used for server-side API proxy to attach cookies)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Must match next.config.ts rewrites — backend root URL (no /api suffix)
BACKEND_URL=http://127.0.0.1:5000
